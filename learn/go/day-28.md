# Day 28: HTTP Basics & Handlers — Request and Response Details

## 1. Read JSON Bodies

Decoded request bodies with `json.NewDecoder`.

## 2. Write JSON Responses

Set `Content-Type: application/json` and encoded structs.

## 3. Use Headers

Read and set custom headers.

## 4. Return Problem Details

Sent consistent JSON error bodies.

## Run

```bash
cd learn/go
go run ./day28
```

## Summary

| Concept | Takeaway |
|---|---|
| Read JSON Bodies | Decoded request bodies with `json |
| Write JSON Responses | Set `Content-Type: application/json` and encoded structs |
| Use Headers | Read and set custom headers |
| Return Problem Details | Sent consistent JSON error bodies |
