# Day 42: Databases (I) — Migrations and Schema Design

## 1. Choose a Migration Tool

Used versioned SQL files checked into `day45/db/schema.sql` pattern.

## 2. Design Tables

Modeled entities with primary keys, timestamps, and indexes.

## 3. Write Up/Down Migrations

Created schema SQL with reversible structure.

## 4. Run Migrations in Dev

Applied schema on startup and verified tables exist.

## Run

```bash
cd learn/go
go run ./day42
```

## Summary

| Concept | Takeaway |
|---|---|
| Choose a Migration Tool | Used versioned SQL files checked into `day45/db/schema |
| Design Tables | Modeled entities with primary keys, timestamps, and ind |
| Write Up/Down Migrations | Created schema SQL with reversible structure |
| Run Migrations in Dev | Applied schema on startup and verified tables exist |
