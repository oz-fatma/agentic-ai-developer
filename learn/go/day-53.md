# Day 53: Auth & Security — Authorization and RBAC

## 1. Define Roles

Modeled `admin` and `reader` roles.

## 2. Authorize Actions

Checked permissions before sensitive operations.

## 3. Return 403

Used Forbidden when authenticated but not permitted.

## 4. Centralize Policy

Authorization helper middleware.

## Run

```bash
cd learn/go
go run ./day53
```

## Summary

| Concept | Takeaway |
|---|---|
| Define Roles | Modeled `admin` and `reader` roles |
| Authorize Actions | Checked permissions before sensitive operations |
| Return 403 | Used Forbidden when authenticated but not permitted |
| Centralize Policy | Authorization helper middleware |
