# Day 25: Testing Fundamentals — Practice

## 1. Test a Package

Full test suite for `calc` with table-driven tests.

## 2. Benchmark a Hot Path

Benchmarked `Pow` function.

## 3. Handler Tests

Tested JSON echo handler with `httptest`.

## 4. Review Failures

Tests cover divide-by-zero and invalid JSON paths.

## Run

```bash
cd learn/go
go test ./day25/...
```

## Summary

| Concept | Takeaway |
|---|---|
| Test a Package | Full test suite for `calc` with table-driven tests |
| Benchmark a Hot Path | Benchmarked `Pow` function |
| Handler Tests | Tested JSON echo handler with `httptest` |
| Review Failures | Tests cover divide-by-zero and invalid JSON paths |
