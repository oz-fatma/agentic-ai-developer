# Day 40: Context, Config & Middleware — Practice

## 1. Upgrade MVP Server

Combined config, context, middleware, and shutdown.

## 2. Request ID Logging

Generated request ID per request in all log lines.

## 3. Shutdown Test

Short lifecycle demo with graceful drain.

## 4. Document Config

Config env vars: PORT, LOG_LEVEL, SHUTDOWN_TIMEOUT.

## Run

```bash
cd learn/go
go run ./day40
```

## Summary

| Concept | Takeaway |
|---|---|
| Upgrade MVP Server | Combined config, context, middleware, and shutdown |
| Request ID Logging | Generated request ID per request in all log lines |
| Shutdown Test | Short lifecycle demo with graceful drain |
| Document Config | Config env vars: PORT, LOG_LEVEL, SHUTDOWN_TIMEOUT |
