# Day 43: Databases (I) — Queries and Prepared Statements

## 1. Use Prepared Statements

Prepared repeated queries for performance.

## 2. Insert Rows

Inserted records with parameterized queries.

## 3. Handle sql.ErrNoRows

Translated missing rows into domain not-found errors.

## 4. Log Query Errors

Wrapped DB errors with operation context.

## Run

```bash
cd learn/go
go run ./day43
```

## Summary

| Concept | Takeaway |
|---|---|
| Use Prepared Statements | Prepared repeated queries for performance |
| Insert Rows | Inserted records with parameterized queries |
| Handle sql.ErrNoRows | Translated missing rows into domain not-found errors |
| Log Query Errors | Wrapped DB errors with operation context |
