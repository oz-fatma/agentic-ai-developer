# Day 38: Context, Config & Middleware — Middleware Chaining

## 1. Context Middleware

Attached request IDs to context.

## 2. Config Middleware

Injected shared dependencies into handlers.

## 3. Compose Chains

Built helper ordering middleware predictably.

## 4. Test Middleware

Asserted headers and context values with httptest.

## Run

```bash
cd learn/go
go run ./day38
```

## Summary

| Concept | Takeaway |
|---|---|
| Context Middleware | Attached request IDs to context |
| Config Middleware | Injected shared dependencies into handlers |
| Compose Chains | Built helper ordering middleware predictably |
| Test Middleware | Asserted headers and context values with httptest |
