# Day 59: Project Layout & Architecture — Domain Models and Services

## 1. Rich Domain Types

Types enforcing invariants, not anemic data bags.

## 2. Validate in Domain

Rejected invalid state before persistence.

## 3. Service Errors

Typed domain errors mapped to transport layer.

## 4. Keep Handlers Thin

Handlers parse, call service, render only.

## Run

```bash
cd learn/go
go run ./day59
```

## Summary

| Concept | Takeaway |
|---|---|
| Rich Domain Types | Types enforcing invariants, not anemic data bags |
| Validate in Domain | Rejected invalid state before persistence |
| Service Errors | Typed domain errors mapped to transport layer |
| Keep Handlers Thin | Handlers parse, call service, render only |
